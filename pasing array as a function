#include<iostream>
using namespace std;

void print(int *myArray)
{
    cout<<sizeof(myArray)<<endl;
}

int main()
{
    
    int n;
    cin>>n;
    int arr[n];
    
    for(int i=0;i<n;i++)
    {
        cin>>arr[i];
    }
    cout<<sizeof(arr)<<endl;
    print(arr);
    
    
}
